name: Update ETF Data Daily

on:
  schedule:
    # 每天北京时间 18:00 运行 (UTC 10:00)
    # 交易日收盘后更新数据
    - cron: '0 10 * * *'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write  # 允许推送提交

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run data update
      env:
        TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
      run: |
        python main.py

    - name: Write update timestamp
      if: success()
      run: |
        echo "{\"last_update\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\", \"update_date\": \"$(date +"%Y-%m-%d")\"}" > last_update.json
        cat last_update.json

    - name: Check for changes
      id: check_changes
      run: |
        if git diff --quiet; then
          echo "has_changes=false" >> $GITHUB_OUTPUT
          echo "No changes detected"
        else
          echo "has_changes=true" >> $GITHUB_OUTPUT
          echo "Changes detected"
        fi

    - name: Commit and push if changed
      if: steps.check_changes.outputs.has_changes == 'true'
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add "主要ETF基金份额变动情况.xlsx" last_update.json
        git commit -m "chore: auto-update ETF data $(date +'%Y-%m-%d')"
        git push
